npm install cd client && npm install cd ../server && npm install cd .. Start the App: From the root directory, run:

npm run dev This will start both the backend (port 3000) and frontend (port 5173) concurrently.

Open in Browser: Visit http://localhost:5173 to see your Name Harmony app!

